\documentclass[titlepage, landscape, a4paper, twocolumn, 10pt]{ujarticle}
\usepackage[top=15truemm,bottom=15truemm,left=10truemm,right=10truemm]{geometry}
\usepackage{listings, src/jlisting}

% set the default code style
\lstset{
  basicstyle={\footnotesize\ttfamily},
  frame=tb, % draw a frame at the top and bottom of the code block
  tabsize=4, % tab space width
  showstringspaces=false, % don't mark spaces in strings
  numbers=left, % display line numbers on the left
  numbersep=1ex, % 行番号とソースコード間のスペース
  lineskip=-0.5ex, % 行間スペース
  xrightmargin=0ex,
  xleftmargin=3ex,
  breaklines=true, % 自動で改行
  columns=[l]{fullflexible}, % 文字間のスペース
}

\begin{document}

\title{ICPC Library}
\author{Whiteboard}
\date{\today}

\maketitle

% insert here

\end{document}
